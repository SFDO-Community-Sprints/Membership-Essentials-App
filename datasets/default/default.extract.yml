
extract:
    Account:
        fields: FIELDS(ALL)
    Contact:
        fields: FIELDS(ALL)
    Product2:
        fields: FIELDS(ALL)
    PriceBookEntry:
        fields: FIELDS(ALL)
    Membership__c:
        fields: FIELDS(ALL)
    Membership_Form_Submission__c:
        fields: FIELDS(ALL)
