<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Is_Active__c</fullName>
    <description>Date-driven test for whether this membership is currently active. Use this in reports rather than Status, which is set to Current on creation and is not advanced automatically.</description>
    <externalId>false</externalId>
    <formula>OR(Does_Not_Expire__c, AND(NOT(ISBLANK(End_Date__c)), End_Date__c &gt;= TODAY()))</formula>
    <formulaTreatBlanksAs>BlankAsBlank</formulaTreatBlanksAs>
    <inlineHelpText>Checked when the membership has not expired, or is marked as never expiring.</inlineHelpText>
    <label>Is Active</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
